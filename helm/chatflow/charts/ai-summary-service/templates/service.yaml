apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-summary-service.fullname" . }}
  labels:
    {{- include "ai-summary-service.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ai-summary-service.selectorLabels" . | nindent 4 }}
