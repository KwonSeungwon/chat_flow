apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "chat-service.fullname" . }}
  labels:
    {{- include "chat-service.labels" . | nindent 4 }}
spec:
  {{- if not .Values.hpa.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "chat-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "chat-service.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      enableServiceLinks: false
      serviceAccountName: {{ include "chat-service.fullname" . }}
      initContainers:
        - name: wait-kafka
          image: busybox
          command: ['sh', '-c', 'until nc -z kafka 9092; do echo "Waiting for Kafka..."; sleep 5; done']
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.global.image.registry }}/{{ .Values.global.image.repository }}/{{ .Values.image.name }}:{{ .Values.global.image.tag }}"
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: {{ .Values.global.springProfile | quote }}
            # JWT Secret (K8s Secret)
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.jwt.secretName }}
                  key: JWT_SECRET
            # PostgreSQL (외부 Secret)
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.postgresql.secretName }}
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.postgresql.secretName }}
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.postgresql.secretName }}
                  key: DB_NAME
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.postgresql.secretName }}
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.postgresql.secretName }}
                  key: DB_PASSWORD
            # Kafka (외부 Secret)
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.kafka.secretName }}
                  key: KAFKA_BOOTSTRAP_SERVERS
            # Valkey (외부 Secret)
            - name: VALKEY_HOST
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.valkey.secretName }}
                  key: VALKEY_HOST
            - name: VALKEY_PORT
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.valkey.secretName }}
                  key: VALKEY_PORT
            - name: VALKEY_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.global.externalSecrets.valkey.secretName }}
                  key: VALKEY_PASSWORD
            - name: FIREBASE_SERVICE_ACCOUNT_PATH
              value: "file:/app/config/firebase/firebase-service-account.json"
          envFrom:
            - configMapRef:
                name: {{ include "chat-service.fullname" . }}
          startupProbe:
            httpGet:
              path: /actuator/health
              port: http
            initialDelaySeconds: 30
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
            periodSeconds: 15
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: firebase-sa
              mountPath: /app/config/firebase
              readOnly: true
            - name: file-uploads
              mountPath: /app/uploads
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: tmp
          emptyDir: {}
        - name: firebase-sa
          secret:
            secretName: chatflow-firebase-secret
            optional: true
        - name: file-uploads
          persistentVolumeClaim:
            claimName: {{ include "chat-service.fullname" . }}-uploads
